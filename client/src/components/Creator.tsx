import React from "react";

const Creator = ({ id: any }) => {
  useEffect(() => {
    first;

    return () => {
      second;
    };
  }, [third]);

  return <div>Creator</div>;
};

export default Creator;
